# ==============================================================
# Experiment 001: Baseline (기본 설정 그대로)
# ==============================================================
# 목적: 성능 기준선 확립 — base.yaml 기본값으로 학습
#
# 실행:
#   python scripts/3_train.py --run-name exp001 --config configs/experiments/exp001_baseline.yaml
#   python scripts/5_submission.py --run-name exp001 --conf 0.25
# ==============================================================

_base_: "../base.yaml"

experiment:
  id: "exp001"
  name: "baseline"
  description: "YOLOv8s 기본 설정으로 56클래스 전체 학습 — 성능 기준선"
  author: "@DM"
  created: "2026-02-05"

# base.yaml 그대로 사용 (변경 없음)

notes: |
  기준선 실험. base.yaml 기본값 그대로 사용.
  - 전체 데이터셋 (232장, 56클래스)
  - YOLOv8s, 768px, 80ep, batch=8
  - 특별한 증강이나 기법 없음
