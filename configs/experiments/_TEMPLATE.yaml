# ==============================================================
# Experiment XXX: 실험 제목 (한줄 요약)
# ==============================================================
# 목적: 이 실험에서 테스트하는 것
#
# 실행:
#   python scripts/3_train.py --run-name expXXX --config configs/experiments/expXXX_name.yaml
#   python scripts/5_submission.py --run-name expXXX --conf 0.25
# ==============================================================

_base_: "../base.yaml"

experiment:
  id: "expXXX"
  name: "실험 이름"
  description: "이 실험에서 테스트하는 것"
  author: "@이름"
  created: "2026-02-XX"

# --- base.yaml에서 변경하는 값만 아래에 작성 ---
# 작성하지 않은 값은 base.yaml 기본값을 자동 상속합니다.
# ⚠️ base.yaml과 같은 값은 적지 마세요 (Override-only 원칙)

# --- 모델/학습 변경 예시 ---
# train:
#   model_name: "yolo11s"    # yolov8n/s/m/l/x, yolo11n/s/m
#   imgsz: 1024              # 해상도 (32의 배수)
#   epochs: 100
#   batch: 4                 # GPU 메모리에 맞게 조절

# --- Augmentation 변경 예시 ---
# train:
#   mosaic: 1.0
#   mixup: 0.15
#   copy_paste: 0.1
#   degrees: 5.0

# --- Optimizer 변경 예시 ---
# train:
#   optimizer: "AdamW"
#   lr0: 0.0005
#   weight_decay: 0.001

# --- Inference threshold 변경 예시 ---
# infer:
#   conf_thr: 0.20

notes: |
  실험 설명과 결과 기록용.
  - 변경 포인트 1
  - 변경 포인트 2
