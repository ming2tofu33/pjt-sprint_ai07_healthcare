_base_: "../baseline.yaml"

model:
  architecture: "yolo11n"
  pretrained: "yolo11n.pt"

train:
  epochs: 30         # 빠른 테스트를 위해 30으로 제한
  imgsz: 640
  batch: 32          # Nano 모델이므로 배치 크기 증가 가능
  patience: 10
  save_period: -1

evaluate:
  visualize: true    # 시각화 활성화
