# smoke test: 3 epochs 소규모 테스트
_base_: "../base.yaml"

model:
  architecture: "yolo11s"
  pretrained: "yolo11s.pt"

train:
  epochs: 3
  imgsz: 640
  batch: 8
  workers: 0
  patience: 3
  verbose: true
  plots: false
  device: 0
